# Only one server-side secret needed — Groq API key.
# Supabase uses a publishable key hardcoded in source (src/lib/supabase.js and api/check.js).
# Get your Groq key at: https://console.groq.com → API Keys

GROQ_API_KEY=your-groq-api-key-here
